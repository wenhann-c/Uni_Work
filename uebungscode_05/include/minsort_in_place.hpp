#pragma once
#include <vector>
// Hier kommt die Deklaration (und Beschreibung) von minsort_in_place hin
void fuelle_zufaellig(std::vector<int>& in);    //generating random numbers

void drucke_array(const std::vector<int>& in);  //print arrays

std::vector<int> int_array_min_sort_in_place(std::vector<int> &input);  //function of min sort in place


